# Week 06 · 6장 관계 만들기
> 테이블을 서로 연결하는 관계(기본키·외래키) 설계하기

## 학습 목표
- 기본키(PRIMARY KEY)와 외래키(FOREIGN KEY)의 역할을 설명할 수 있다.
- 1:N 관계를 외래키로 표현할 수 있다.
- 새 테이블을 관계와 함께 설계할 수 있다.

## 실습 과제
1. 도서관에 '예약(reservation)' 기능을 추가하려 합니다. 회원이 도서를 예약한 기록을 담는 reservation 테이블을 CREATE TABLE로 설계하세요. (회원·도서를 외래키로 연결)
2. 위 reservation에서 외래키가 왜 필요한지 한 문장으로 설명하세요. (주석으로 작성)
3. member와 loan은 몇 대 몇(1:N 등) 관계인가요? 이유와 함께 주석으로 설명하세요.

## 제출
`answer.sql`에 작성 → 결과 캡처(`result.png`) 첨부 → Commit & Push.
